import * as api from './api.js';
import {imHttpService,httpService,currentHost,http} from './http.js';
console.log('api ---- ', api, imHttpService,httpService,currentHost,http)
export {
    api,
    imHttpService,
    httpService,
    http,
    currentHost
}
